# Default behavior, in case core.autocrlf is not set.

* text=auto

# Text files that will always be normalized and converted to native line
# endings on checkout.

*.java text
*.properties text

# Text files that will always have CRLF (Windows) line endings on checkout.

*.bat text eol=crlf

# All files that are binary and should not be modified.

# *.png binary
# *.jpg binary

